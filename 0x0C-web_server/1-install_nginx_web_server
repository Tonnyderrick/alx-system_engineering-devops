#!/usr/bin/env bash
# This script installs and configures nginx to return "Hello World!" at /

# Update package list and install nginx silently
apt-get update -y
apt-get install nginx -y

# Replace default nginx index with Hello World!
echo "Hello World!" > /var/www/html/index.html

# Start nginx using service (not systemctl)
service nginx start
